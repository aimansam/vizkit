[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "build"
version = "1.0.0"
description = "{{DESC}}"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
dependencies = {{}}
